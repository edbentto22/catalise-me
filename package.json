{
  "name": "catalise-me-site",
  "type": "module",
  "version": "2.0.0",
  "description": "Site institucional da Catalise.me — Astro",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "astro preview --host 0.0.0.0 --port ${PORT:-4321}"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.10.4",
    "lenis": "^1.3.25",
    "lucide": "^1.16.0"
  }
}
